import React from 'react';

function Card({project}) {

    const {name, description, techStack, link, repo} = project;

    return (
        <div>
            
        </div>
    );
}

export default Card;